node_modules/
dist/
out/
build/
# …except electron-builder's buildResources, which is source, not output.
!apps/desktop/build/
.turbo/
coverage/
*.tsbuildinfo

# Electron packaging output
apps/desktop/out/
apps/desktop/dist/
# Built apps and DMGs — attached to a GitHub release, never committed.
apps/desktop/release/

# Generated Codex protocol bindings are committed on purpose (see plan §6.1).
# Nothing else generated should be.

# Local data
*.db
*.db-shm
*.db-wal
.chorus/

# Environment / secrets — never commit
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Built by apps/vscode-extension/package.mjs and bundled as an app resource.
*.vsix
